import {Meteor} from 'meteor/meteor';
import {StatsBeginner} from '../../lib/collections'

// Meteor.publish('statsBeginner'=> {
//   return StatsBeginner.find({}, {limit: 20, sort: {lastPost: -1}});
// });
